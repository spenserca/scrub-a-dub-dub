describe('when decrypting from a string', () => {
  it.todo('gets the decrypted object');

  it.todo('returns the decrypted object as its string representation');
});

describe('encrypting to an object', () => {
  it.todo('gets the decrypted object');

  it.todo('returns the object with decrypted properties');
});
